import React from "react";

const Details = (props) => {
  return (
    <h1>winwdinwdindwi</h1>
    //This is a nice way to debug props/state etc
    // <pre>
    //   <code>{JSON.stringify(props, null, 4)}</code>
    // </pre>
  );
};

export default Details;
